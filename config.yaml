# config.yaml

postgres:
  database: cti_breach_hunter
  user: cti_user
  password: cti_password
  host: localhost
  port: 5432

elasticsearch:
  host: localhost
  port: 9200

settings:
  system_tick: 0.1
  should_update: true
